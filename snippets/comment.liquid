<!-- /snippets/comment.liquid -->
<div class="comment__content rte">
  {{ comment.content }}
</div>
<div class="comment__meta">
  <span class="comment__meta-item authour">{{ comment.author }}</span>
  <span class="comment__meta-item time">{{ comment.created_at | time_tag: format: 'month_day_year' }}</span>
</div>
